# Project Maties WebView2 Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development for independent tasks and a final review.

**Goal:** Preserve v5.5 functionality with a small shared-runtime Windows app and private GitHub delivery.
**Architecture:** Native WPF host, reusable JS runtime engine, untouched exact wheel renderer/assets, existing C# input service.
**Tech Stack:** .NET Framework 4.8, Microsoft WebView2 WPF CompositionControl, JavaScript ES modules, SDL3, Node for development only.
**Spec:** docs/superpowers/specs/2026-09-11-webview2-design.md

## Global Constraints
- Preserve every v5.5 feature and exact radial wheel SVG geometry.
- Shared WebView2 runtime accepted; do not bundle Chromium, Electron or Node in release.
- Keep AppData/ControllerCompanion configuration filenames and schemas compatible.
- Private repository NorthAngel/project-maties; use version 0.6.0.
- No desktop/input injection outside isolated test targets; root coordinates foreground tests.
- Source at C:/Users/Northangle/Downloads/project/1/project-maties, release next to it.

### Task 1: Extract platform-independent runtime and browser bridge
Files: src/runtime-engine.js, src/webview-bridge.js, src/runtime-entry.js, tests/runtime-engine.test.js; minimal settings.html/overlay.html script integration.
Interface: RuntimeEngine(platform, bootstrap); platform.send(command), emit(event,payload), overlay(active,packet,appearance,layout), saveAppearance(value), controller(command), saveControllers(value). Public getRuntime(), getAppearance(), setAppearance(value), setEnabled(value), feed(packet), stop(), native(message), controllerAction(value), setMaintenance(value). Native bootstrap contains appearance, controllers, ownPid, testing. Preserve current desktop.cjs behavior.
- [ ] Add behavior tests for L3/dual/triple, modifiers, controller requests, persistence and stopped/disconnected state.
- [ ] Extract current orchestration around existing InputSession and PointerInput unchanged.
- [ ] Use WebView messages {id,method,args} / {id,result,error}, events {event,payload}. UI window.desktop signatures remain unchanged.
- [ ] Verify Node tests and report exact files, commits, tests, limitations.

### Task 2: Native maintenance and input-service lifecycle
Files: host/NativeService.cs, host/Maintenance.cs. Consumes root host interface defined in task brief. Preserve native helper line protocol. Keep repair/update integrity and rollback. No GUI required for this task.
- [ ] Implement helper start/send/stop/restart, exit notification and stdout messages with UI-thread dispatch delegated to host.
- [ ] Implement SHA256 manifests, required files, safe relative paths and no symlinks, version/product/PE checks and staged repair rollback.
- [ ] Test corruption, missing required file, path traversal, invalid version, repair backup and process stop.

### Task 3: Native WebView2 desktop host and packaging (root)
Files: host/ControllerCompanion.csproj, host/App.cs, host/WebWindow.cs, host/HostPlatform.cs, scripts/build-host.ps1, scripts/package.ps1.
- [ ] Prove transparent composition overlay with exact SVG content, nonactivation and click-through before full integration.
- [ ] Implement allowlisted bridge, settings/tray/single-instance/theme/config/capture/window/maintenance hooks and JS engine relay.
- [ ] Build x64 with pinned NuGet dependencies and framework references; copy only required runtime DLLs and actively used assets.
- [ ] Compress recovery payload without reducing repair coverage; manifest all release files.
- [ ] Confirm missing WebView2/runtime messaging and publish measured byte comparison.

### Task 4: Parity, documentation and GitHub delivery (root + review)
Files: tests/webview-desktop.mjs, docs/architecture.md, docs/feature-parity.md, README.md, CHANGELOG.md, CONTRIBUTING.md, SECURITY.md, .github/workflows/ci.yml, .github/ISSUE_TEMPLATE/*.
- [ ] Run existing logic/native checks, new bridge/maintenance checks and full real Windows parity tests including IME and transparency.
- [ ] Final reviewer checks implementation against spec and tests; fix concrete findings.
- [ ] Exclude caches, generated screenshots, personal paths, credentials, unused demo images and bundled browser files from Git.
- [ ] Commit reviewable baseline and refactor commits, create/push private repository, verify CI and create tagged release.
- [ ] Launch new verified app from requested directory, archive/source checksums, then remove superseded deliverables under prior authorized cleanup policy.
