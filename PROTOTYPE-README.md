# Controller Companion v5.5

Windows 10 / 11 x64 便携式手柄转盘输入。保持程序文件夹完整，运行 ControllerCompanion.exe；右下角通知区域图标可再次打开设置。

## 界面与系统设置

- 支持简体中文、繁體中文、English、日本語、Français、Deutsch、Italiano、Español。语言切换立即生效。
- 在左侧菜单的「系统设置」中选择「跟随系统／浅色／深色」。设置页、对话框和默认转盘配色同步调整；自定义色值继续保留。
- 原左下角的后台输入开关与退出入口移入系统设置。可设置开机自动运行、关闭窗口时隐藏到托盘／最小化／退出。
- 右上角「最小化」真正最小化到任务栏。窗口不提供最大化按钮；可拖动标题区域移动窗口。

## 手柄操作

- **单按 L3**：进入单转盘，再次单按收起。为识别双摇杆组合，长按时等待约 300 ms；短按释放即可确认。
- **L3 + R3**：两个按下动作间隔不超过 300 ms、并有同时按住的时刻，进入双转盘。左右先后均可；再次组合收起。可在活动中直接从单盘切到双盘或反向切换。
- **连续三次短按 R3**：切换 Windows 已安装的输入语言，优先中／英／日。直接使用 Windows 默认输入法、拼音／罗马字转换与系统候选词窗口，不提供自建输入法。
- 为区分三击，单次 R3 点按的自定义动作会稍后确认；组合和三击不会附带鼠标中键点击。
- 单盘：左摇杆选区，XYAB 确认上／下／左／右字符，右摇杆移动鼠标。十字键默认提供空格、退格、回车和 Shift。
- 双盘：左摇杆与十字键控制左盘；右摇杆与 XYAB 控制右盘。仍使用原 Figma 几何和实体按键布局。
- 转盘收起：左摇杆控制鼠标；右摇杆持续向上／下／左／右推住即可持续滚动，推得越大越快。细小滚动量、平滑加速、方向抑制和回中停止用于减少抖动。
- Escape 收起转盘。目标窗口改变、手柄断开、暂停或校准时会停止输入并释放按住的鼠标键及修饰键。

## Windows 输入法

请先在 Windows「时间和语言 → 语言和区域」安装需要的语言与输入法。三击 R3 切换的是系统已安装项；它不安装、下载或替换输入法。中文与日文转盘字母交给当前系统输入法组合。空格、回车以及自定义方向键可操作 Windows 候选窗口，候选行为遵循所用输入法。

日文沿用 Windows 自身的 A／あ 输入模式。若当前显示 A，请在系统输入法菜单选择平假名，或使用 Windows 的 Ctrl + Caps Lock 快捷键；三击 R3 切换语言时不覆盖你的输入模式偏好。参考：https://support.microsoft.com/en-us/windows/hardware/input-devices/microsoft-japanese-ime

部分以管理员身份运行的应用拒绝普通权限的模拟输入。不同应用和第三方输入法可能存在兼容差异；软件不强制提升权限或抢夺前台焦点。

## 自助修复与更新

- 「检查组件」检查输入服务、SDL3、手柄识别数据库，并显示服务和连接状态。
- 「修复输入服务」先校验附带的恢复副本，再恢复损坏或缺失的输入组件并重启服务；保留外观、按键和手柄校准。
- 「软件更新」选择收到并解压的**新版程序文件夹**（含 release-manifest.json）。验证产品、版本和全部清单文件 SHA-256 后，确认即可重启到所选版本。此便携版尚未配置在线发布源，不会伪报已获取在线最新版。
- 清单用于完整性检查，不是发行者数字签名；请选择可信来源的新版本。新版目录需要保留；已启用的开机启动会同步使用新版路径。
- 「固件支持」打开当前设备品牌的官方说明。自制 RP2040 的固件由具体硬件和固件项目决定，不通刷通用固件；未知型号会提示联系提供者。

## 手柄识别与配置

延续 v5.3 的 SDL3、HIDAPI、Windows.Gaming.Input、XInput 支持。未知标准 USB HID 可通过「手柄 → 校准按键」映射；未完成必要校准时暂停此设备的键鼠输入。PS5 使用 △ / × / □ / ○，不改变物理位置。

设置保存在 %APPDATA%/ControllerCompanion：appearance.json（外观与按键）、controllers.json（手柄映射）、system.json（语言、主题和窗口行为）。更新版本不会清除这些文件。

## 源码与验证

源码从 https://github.com/htlin222/web-gamepad-starter 延续修改，原许可保留。使用 Electron + 原生 C# SendInput 桥接 + SDL3。ControllerCompanion.sln 可在 Visual Studio 打开；package.json 中提供构建和测试入口。

本版具体测试结果和限制见 docs/验证记录-v05.5.md。旧版测试记录保留在当前源码文档中；新版本验证完成后按默认交付策略仅保留最新交付物。
